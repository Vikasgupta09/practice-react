/**
 * React Component
 * ---------------
 * is a function or a class
 * that produces HTML to show the user(using JSX)
 * and handles feedback from the user(using event handlers).
 */

/**
 * Function vs Class based component
 *
 * Function based component
 * ------------------------
 * should be used for simple component
 *
 * Class based component
 * ---------------------
 * should be used for just about anything else
 *
 * - Easier code organization
 * - Can use 'state' system
 * - understands life cycle events
 */

/**
 * JSX
 * ---
 * Special dialect of JS
 * JSX code is converted into JS using tools (babel)
 * Very similar in form to HTML with couple of differences:
 *
 * - custom styling use different syntax
 * HTML:
 *  <div style="background-color: red"></div>
 * JSX:
 *  <div style= {{ backgroundColor: 'red' }}></div>
 *
 * - Adding a class use different syntax (also Adding for loop use different syntax)
 * HTML:
 *  <div class = "nav"></div>
 * JSX:
 *  <div className = "nav"></div>
 *
 * - JSX can reference JS variables/function()
 * using {  }
 *
 */
